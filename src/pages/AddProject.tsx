import Onboarding from "@/pages/Onboarding";

const AddProject = () => {
  return <Onboarding />;
};

export default AddProject;
export type { ProjectData, RequirementItem } from "@/pages/Onboarding";
